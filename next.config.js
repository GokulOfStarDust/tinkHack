/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["assets.aceternity.com"], // Add the allowed image domain here
    },
  };
  
  module.exports = nextConfig;
  